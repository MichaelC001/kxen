---
import { Icon } from "astro-icon/components";
import { Button } from "./ui/button";
import { LinkButton } from "./ui/link-button";
import { ThemeToggle } from "./ui/theme-toggle";
import { SearchTrigger } from "./ui/search";
import { config } from "virtual:nimbus/config";

interface Props {
  collection?: string;
  entryId?: string;
  /** false 时隐藏移动端菜单（sidebar:false 的页没有可开的侧栏）。 */
  showSidebar?: boolean;
}

// collection/entryId 预留给版本化文档，当前导航未消费。
const { showSidebar = true } = Astro.props;

const currentSlug = Astro.url.pathname.replace(/\/$/, "") || "/";
const primaryNav = [
  { label: "产品", href: "/overview", active: currentSlug.startsWith("/overview") },
  {
    label: "开始使用",
    href: "/getting-started",
    active: currentSlug.startsWith("/getting-started"),
  },
  {
    label: "文档",
    href: "/workspace",
    active:
      currentSlug !== "/" &&
      !currentSlug.startsWith("/overview") &&
      !currentSlug.startsWith("/getting-started"),
  },
];
---

<header class="sticky top-0 z-30 border-b border-border bg-card/80 backdrop-blur-lg">
  <div class="mx-auto flex items-center justify-between h-14 px-4 lg:px-6">
    <div class="flex items-center gap-3">
      <a href="/" class="group">
        <span class="flex items-center gap-2 font-semibold text-sm text-foreground group-hover:text-primary transition-colors">
          <img src="/icon.png" width="24" height="24" alt="" class="h-6 w-6 rounded-md" />
          {config.title}
        </span>
      </a>

      <nav aria-label="主要导航" class="hidden md:flex items-center gap-1 ml-4 pl-4 border-l border-border h-6">
          {primaryNav.map((item) => (
            <a
              href={item.href}
              aria-current={item.active ? "page" : undefined}
              class:list={[
                "px-2 py-1 rounded-md text-[0.8125rem] font-medium no-underline transition-colors",
                item.active
                  ? "text-foreground bg-accent"
                  : "text-muted-foreground hover:text-foreground hover:bg-accent/60",
              ]}
            >
              {item.label}
            </a>
          ))}
      </nav>
    </div>

    <div class="flex items-center gap-2">
      {config.search !== false && <SearchTrigger />}
      {config.github && (
        <LinkButton
          href={config.github}
          variant="minimal"
          shape="square"
          size="sm"
          aria-label="GitHub"
          target="_blank"
          rel="noopener"
          class="text-muted-foreground"
        >
          <Icon name="ph:github-logo" class="w-4 h-4" />
        </LinkButton>
      )}
      <ThemeToggle />
      {showSidebar && (
        <Button
          variant="ghost"
          shape="square"
          size="sm"
          icon="ph:list"
          aria-label="打开导航"
          data-menu-btn
          class="lg:hidden text-muted-foreground"
        />
      )}
    </div>
  </div>
</header>
