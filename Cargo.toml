[workspace]
members = ["crates/kxen-agent", "crates/kxen-core", "crates/kxen-cli", "src-tauri"]
resolver = "3"

[profile.release]
opt-level = 3
# PRD 3.6 全 LTO（fat）：codegen-units=1 已就位，thin 换 fat 换最后一段内联收益
lto = true
codegen-units = 1
strip = true
panic = "abort"
